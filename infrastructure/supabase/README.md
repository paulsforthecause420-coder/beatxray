# Supabase Infrastructure
The authoritative schema is `supabase/migrations`. Dashboard-only changes are prohibited. Use `supabase db diff` to capture intentional changes and review generated SQL before committing.
